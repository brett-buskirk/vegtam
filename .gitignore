.DS_Store
*.log

# python bytecode from the dev-only screenshot generator
__pycache__/
*.pyc

# local agent settings — machine-specific, never shared
.claude/settings.local.json

# secrets — never commit these
.env
.env.*
*.pem
*.key
