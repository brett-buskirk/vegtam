# Contributing

- **No direct commits to `main`** — branch → PR → green checks → merge.
- **AgentGate runs on every PR** (if configured) — `secrets` + `dangerous_patterns` block.
- Never commit secrets (`.env`, keys are gitignored).
